---
const { className, contentClassName, id } = Astro.props
---

<div id={id} class={`rounded-xl p-1 text-sm border border-gray-200 min-w-80 min-h-80 grid grid-cols-1 grid-rows-[auto_1fr] ${className}`}>
    <div class="flex gap-2 p-2">
        <span class="size-3 rounded-full bg-black/20"></span>
        <span class="size-3 rounded-full bg-black/20"></span>
        <span class="size-3 rounded-full bg-black/20"></span>
    </div>
    <div class={`rounded-lg bg-black/5 font-mono p-2 ${contentClassName}`}>
        <slot/>
    </div>
</div>