import { c as createComponent, d as createAstro, r as renderComponent, b as renderTemplate, m as maybeRenderHead, e as addAttribute } from '../../chunks/astro/server_GOUyW78n.mjs';
import 'piccolore';
import { g as getCollection, b as $$Container, a as last_projects_title, $ as $$ContainerTitle, p as projects_no_image, l as last_projects_no_items } from '../../chunks/ContainerTitle_DfSgzJDH.mjs';
import { $ as $$Layout, a as $$LinkButton } from '../../chunks/Layout_Dnv8xzrq.mjs';
import { g as getLocale, t as trackMessageCall } from '../../chunks/runtime_CvrDTCWp.mjs';
import { m as months_present } from '../../chunks/months_present_BT5Gp1RR.mjs';
export { renderers } from '../../renderers.mjs';

/* eslint-disable */
/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

const en_projects_project_status = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Project status`)
};

const es_projects_project_status = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Estado del proyecto`)
};

/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
* 
* @param {{}} inputs
* @param {{ locale?: "en" | "es" }} options
* @returns {LocalizedString}
*/
/* @__NO_SIDE_EFFECTS__ */
const projects_project_status = (inputs = {}, options = {}) => {
	const locale = options.locale ?? getLocale();
	trackMessageCall("projects_project_status", locale);
	if (locale === "en") return en_projects_project_status()
	return es_projects_project_status()
};

/* eslint-disable */
/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

const en_projects_used_technologies = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Used technologies`)
};

const es_projects_used_technologies = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tecnologías utilizadas`)
};

/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
* 
* @param {{}} inputs
* @param {{ locale?: "en" | "es" }} options
* @returns {LocalizedString}
*/
/* @__NO_SIDE_EFFECTS__ */
const projects_used_technologies = (inputs = {}, options = {}) => {
	const locale = options.locale ?? getLocale();
	trackMessageCall("projects_used_technologies", locale);
	if (locale === "en") return en_projects_used_technologies()
	return es_projects_used_technologies()
};

/* eslint-disable */
/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

const en_projects_roles = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Roles`)
};

const es_projects_roles = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Roles`)
};

/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
* 
* @param {{}} inputs
* @param {{ locale?: "en" | "es" }} options
* @returns {LocalizedString}
*/
/* @__NO_SIDE_EFFECTS__ */
const projects_roles = (inputs = {}, options = {}) => {
	const locale = options.locale ?? getLocale();
	trackMessageCall("projects_roles", locale);
	if (locale === "en") return en_projects_roles()
	return es_projects_roles()
};

/* eslint-disable */
/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

const en_projects_urls = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Urls`)
};

const es_projects_urls = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Enlaces`)
};

/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
* 
* @param {{}} inputs
* @param {{ locale?: "en" | "es" }} options
* @returns {LocalizedString}
*/
/* @__NO_SIDE_EFFECTS__ */
const projects_urls = (inputs = {}, options = {}) => {
	const locale = options.locale ?? getLocale();
	trackMessageCall("projects_urls", locale);
	if (locale === "en") return en_projects_urls()
	return es_projects_urls()
};

/* eslint-disable */
/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

const en_projects_date = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Date`)
};

const es_projects_date = /** @type {(inputs: {}) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Fecha`)
};

/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
* 
* @param {{}} inputs
* @param {{ locale?: "en" | "es" }} options
* @returns {LocalizedString}
*/
/* @__NO_SIDE_EFFECTS__ */
const projects_date = (inputs = {}, options = {}) => {
	const locale = options.locale ?? getLocale();
	trackMessageCall("projects_date", locale);
	if (locale === "en") return en_projects_date()
	return es_projects_date()
};

const $$Astro = createAstro();
async function getStaticPaths() {
  try {
    const allProjects = await getCollection("projects");
    if (!allProjects || allProjects.length === 0) {
      return [];
    }
    const paths = [];
    const processedProjects = /* @__PURE__ */ new Set();
    allProjects.forEach((item) => {
      try {
        const projectName = item.id.split("/")[0];
        const projectKey = `${projectName}`;
        if (processedProjects.has(projectKey)) {
          return;
        }
        processedProjects.add(projectKey);
        paths.push({
          params: { project: projectName },
          props: {
            projectName,
            defaultLocale: "es"
          }
        });
      } catch (error) {
        console.error(`Error procesando proyecto ${item.id}:`, error);
      }
    });
    console.log(`\u2705 Rutas generadas: ${paths.length} proyectos`);
    return paths;
  } catch (error) {
    console.error("\u274C Error en getStaticPaths:", error);
    return [];
  }
}
const $$ = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$;
  const { project } = Astro2.params;
  const currentLocale = getLocale() || "es";
  if (!project) {
    return Astro2.redirect("/projects");
  }
  const allProjects = await getCollection("projects");
  function getLocaleFromId(id) {
    const filename = id.split("/")[1] || "";
    if (filename.includes("-en"))
      return "en";
    if (filename.includes("-es"))
      return "es";
    return "es";
  }
  let foundProject = null;
  let projectData = null;
  console.log(`\u{1F50D} Buscando proyecto: ${project} (locale: ${currentLocale})`);
  console.log(`\u{1F4C1} Total proyectos disponibles: ${allProjects.length}`);
  allProjects.forEach((item, index) => {
    const projectName = item.id.split("/")[0];
    const projectLocale = item.data.lang || getLocaleFromId(item.id);
    console.log(`   ${index + 1}. ${item.id} -> nombre: ${projectName}, locale: ${projectLocale}`);
  });
  for (const item of allProjects) {
    const projectName = item.id.split("/")[0];
    const projectLocale = item.data.lang || getLocaleFromId(item.id);
    if (projectName === project && projectLocale === currentLocale) {
      foundProject = item;
      console.log(`\u2705 Proyecto encontrado: ${item.id}`);
      break;
    }
  }
  if (!foundProject) {
    console.log(`\u274C Proyecto no encontrado: ${project} (${currentLocale})`);
    const alternativeVersions = allProjects.filter((item) => {
      const projectName = item.id.split("/")[0];
      return projectName === project;
    });
    if (alternativeVersions.length > 0) {
      console.log(`\u{1F4A1} Versiones alternativas encontradas:`);
      alternativeVersions.forEach((v) => {
        const lang = v.data.lang || getLocaleFromId(v.id);
        console.log(`   - ${lang}: ${v.id}`);
      });
    }
    return notFound();
  }
  let Content = null;
  let headings = [];
  try {
    const rendered = await foundProject.render();
    Content = rendered.Content;
    headings = rendered.headings || [];
    console.log(`\u{1F4DD} Contenido renderizado exitosamente`);
  } catch (error) {
    console.error("\u274C Error rendering project:", error);
  }
  const projectVersions = allProjects.filter((item) => {
    const projectName = item.id.split("/")[0];
    return projectName === project;
  }).map((item) => ({
    lang: item.data.lang || getLocaleFromId(item.id),
    url: `/projects/${project}`
    // Astro maneja el prefijo de idioma automáticamente
  }));
  console.log(`\u{1F310} Versiones disponibles: ${projectVersions.map((v) => v.lang).join(", ")}`);
  projectData = {
    id: foundProject.id,
    slug: project,
    title: foundProject.data.title || "Sin t\xEDtulo",
    description: foundProject.data.description || "",
    lang: foundProject.data.lang || getLocaleFromId(foundProject.id),
    technologies: foundProject.data.technologies || [],
    urls: foundProject.data.urls || [],
    coverImage: foundProject.data.coverImage,
    coverAlt: foundProject.data.coverAlt || foundProject.data.title,
    date: foundProject.data.date || {},
    role: foundProject.data.role,
    status: foundProject.data.status || "finished",
    featured: Boolean(foundProject.data.featured),
    client: foundProject.data.client,
    category: foundProject.data.category,
    duration: foundProject.data.duration
  };
  `${projectData.title} | Proyectos`;
  projectData.description || `Detalles del proyecto ${projectData.title}`;
  projectData.coverImage || "/images/default-project.jpg";
  const canonicalUrl = new URL(Astro2.url);
  canonicalUrl.pathname = `/projects/${project}`;
  let seo = {
    title: `${projectData.title} | Gerardo Gallegos Jr`,
    description: projectData.description,
    openGraph: {
      basic: {
        title: `${projectData.title} | Gerardo Gallegos Jr`,
        type: "website",
        image: projectData.coverImage
      }
    },
    extend: {
      // link: [{ rel: "icon", href: "/favicon.ico" }],
      meta: [
        // {
        // name: "twitter:image",
        // content: "https://user-images.githubusercontent.com/5182256/131216951-8f74f425-f775-463d-a11b-0e01ad9fce8d.png",
        // },
        // { name: "twitter:title", content: "Tinker Tailor Soldier Spy" },
        // { name: "twitter:description", content: "Agent" },
        { name: "description", content: projectData.description }
      ]
    }
  };
  return renderTemplate`<!-- El resto del código HTML/JSX permanece igual -->${renderComponent($$result, "Layout", $$Layout, { "seo": seo }, { "default": async ($$result2) => renderTemplate` ${renderComponent($$result2, "Container", $$Container, { "className": "flex flex-col items-start justify-center pt-20" })} ${renderComponent($$result2, "Container", $$Container, { "className": "min-h-14 flex flex-row items-start justify-start" }, { "default": async ($$result3) => renderTemplate` ${maybeRenderHead()}<a${addAttribute("/projects", "href")} class="flex flex-row gap-2 items-center justify-center w-fit text-gray-400"> <span class="material-symbols-rounded">
chevron_left
</span> ${last_projects_title()} </a> ` })} ${renderComponent($$result2, "ContainerTitle", $$ContainerTitle, { "title": projectData.title, "subtitle": "main-title" })} ${renderComponent($$result2, "Container", $$Container, { "className": "w-full flex flex-col-reverse lg:grid lg:grid-cols-5 gap-12 relative py-2" }, { "default": async ($$result3) => renderTemplate` <div class="col-span-3 w-full h-fit max-w-xl text-base/7 text-gray-500 pb-12 [&_ul]:list-disc [&_ul]:pl-6 [&_h1,h2,h3]:w-full [&_h1,h2,h3]:text-lg [&_h1,h2,h3]:font-bold [&_h1,h2,h3]:text-black"> ${renderComponent($$result3, "Content", Content, {})} </div> <div class="col-span-2 w-full h-fit flex gap-2 flex-col items-center justify-start friendly-sticky top-0 text-base/7 text-gray-500 pb-12"> <div class="aspect-video relative w-full border border-[--text-grayRGBA] overflow-hidden group flex items-center justify-center"> ${projectData.coverImage ? renderTemplate`<img${addAttribute(projectData.coverImage, "src")}${addAttribute(projectData.coverAlt, "alt")} class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 ease-in-out border-none">` : renderTemplate`<div class="absolute bg-gray-200 w-full h-full text-[--text] text-lg flex flex-col items-center justify-center"> <span class="material-symbols-rounded text-2xl">
image
</span> <span>${projects_no_image}</span> </div>`} </div> <span class="w-full text-lg font-bold text-black">${projects_project_status()}</span>  <div class="w-full flex-none flex flex-wrap gap-2 text-sm items-center justify-start overflow-y-auto overflow-x-hidden"> <span class="capitalize text-xs/6 font-mono px-2 bg-blue-500/70 text-white rounded-full flex-none flex items-center justify-center gap-1"> ${projectData.status} </span> </div> <span class="w-full text-lg font-bold text-black">${projects_used_technologies()}</span> <div class="w-full flex-none flex flex-wrap gap-2 text-sm items-center justify-start overflow-y-auto overflow-x-hidden"> ${projectData.technologies.length > 0 && renderTemplate`<div class="w-full flex flex-row flex-wrap gap-2 text-sm items-center justify-start overflow-y-auto overflow-x-hidden"> ${projectData.technologies.map((tech, index) => renderTemplate`<span class="capitalize text-xs/6 font-mono px-2 bg-blue-500/70 text-white rounded-full flex-none flex items-center justify-center gap-1"> <i${addAttribute("icon icon-" + tech, "class")}></i> ${tech.replace("-", " ")} </span>`)} </div>`} </div> <span class="w-full text-lg font-bold text-black">${projects_roles()}</span> <p class="w-full text-lg">${projectData.role}</p> <span class="w-full text-lg font-bold text-black">${projects_urls()}</span> <div class="w-full flex-none flex flex-wrap gap-2 text-sm items-center justify-start overflow-y-auto overflow-x-hidden"> ${projectData.urls.length === 0 ? renderTemplate`<span class="text-lg w-full text-[--text-gray]">${last_projects_no_items()}</span>` : ""} ${projectData.urls.map(({ name, url }) => renderTemplate`${renderComponent($$result3, "LinkButton", $$LinkButton, { "target": "_blank", "width": "[14rem]", "extraClasses": "!text-base !py-1", "id": "linkedinButton", "href": url }, { "default": async ($$result4) => renderTemplate`${name}<span class="material-symbols-rounded">
captive_portal
</span> ` })}`)} </div> <span class="w-full text-lg font-bold text-black">${projects_date()}</span> <div class="w-full text-lg"> <div class="meta-item"> <span class="meta-value"> ${projectData.date?.start && (projectData.date.start === months_present().toLowerCase() ? months_present() : new Date(projectData.date.start).toLocaleDateString(currentLocale))} ${projectData.date?.end && " - "} ${projectData.date?.end && (projectData.date.end === months_present().toLowerCase() ? months_present() : new Date(projectData.date.end).toLocaleDateString(currentLocale))} </span> </div> </div> </div> ` })} ` })}`;
}, "/Users/gerag18/Documents/GitHub-HT/Portfolio-V2/src/pages/projects/[...project].astro", void 0);

const $$file = "/Users/gerag18/Documents/GitHub-HT/Portfolio-V2/src/pages/projects/[...project].astro";
const $$url = "/projects/[...project]";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
	__proto__: null,
	default: $$,
	file: $$file,
	getStaticPaths,
	url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
